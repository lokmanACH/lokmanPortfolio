// Legacy Lovable reporting hooks were removed to keep the project standalone.
// This file intentionally does not reference any editor-specific runtime hooks.

export function reportRuntimeError(
  _error: unknown,
  _context: Record<string, unknown> = {},
) {
  // Intentionally left blank.
}
